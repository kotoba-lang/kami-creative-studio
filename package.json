{
  "name": "kami-creative-studio",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "release": "rm -rf public && mkdir -p public/js public/vendor && shadow-cljs release app && clojure -X:pages build/release && cp -R node_modules/@google/model-viewer/dist public/vendor/model-viewer",
    "check": "clojure -M:test"
  },
  "dependencies": {
    "@google/model-viewer": "4.3.1",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "shadow-cljs": "2.28.20"
  }
}
